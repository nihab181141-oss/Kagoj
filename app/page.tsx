'use client'

import { useMemo, useState } from 'react'
import {
  ArrowLeft, ArrowUpRight, Check, ChevronDown, Download, FileCheck2, FilePlus2, FileText,
  Layers3, LayoutTemplate, Menu, MoreHorizontal, Palette, Pencil, Plus, ReceiptText,
  Search, Send, Settings2, Share2, Sparkles, Stamp, Type, Upload, WalletCards, X, Zap,
} from 'lucide-react'

const types = [
  { name: 'Invoice', icon: FileText, desc: 'Request a payment', accent: 'coral' },
  { name: 'Receipt', icon: ReceiptText, desc: 'Confirm a payment', accent: 'teal' },
  { name: 'Quotation', icon: FileCheck2, desc: 'Share an estimate', accent: 'gold' },
  { name: 'Payment confirmation', icon: WalletCards, desc: 'Mark it paid', accent: 'blue' },
  { name: 'Order confirmation', icon: Stamp, desc: 'Lock in an order', accent: 'violet' },
  { name: 'Delivery note', icon: Layers3, desc: 'Track a delivery', accent: 'lime' },
]
const templates = ['Minimal', 'Professional', 'Modern', 'Luxury', 'Creative', 'Corporate', 'Retail', 'Service']
const docs = [
  { title: 'Invoice #INV-024', client: 'Aarong Interiors', amount: '৳ 48,500', status: 'Sent', tint: 'coral' },
  { title: 'Receipt #REC-018', client: 'North End Coffee', amount: '৳ 12,800', status: 'Paid', tint: 'teal' },
  { title: 'Quote #QUO-009', client: 'Bengal Logistics', amount: '৳ 86,200', status: 'Draft', tint: 'gold' },
]

type View = 'home' | 'workspace' | 'editor' | 'templates' | 'brand' | 'success'

function Logo() { return <div className="logo-lockup"><div className="brand-mark">K</div><span>kagoj</span><small>studio</small></div> }
function Nav({ view, setView }: { view: View; setView: (v: View) => void }) {
  return <header className="app-header"><button className="icon-button mobile-only" aria-label="Open menu"><Menu size={18}/></button><button className="logo-button" onClick={() => setView('home')}><Logo /></button><nav className="top-nav"><button className={view === 'home' ? 'active' : ''} onClick={() => setView('home')}>Home</button><button className={view === 'workspace' ? 'active' : ''} onClick={() => setView('workspace')}>Workspace</button><button className={view === 'templates' ? 'active' : ''} onClick={() => setView('templates')}>Templates</button><button className={view === 'brand' ? 'active' : ''} onClick={() => setView('brand')}>Brand kit</button></nav><div className="header-actions"><button className="icon-button" aria-label="Search"><Search size={18}/></button><button className="avatar">RA</button><button className="icon-button" aria-label="Settings"><Settings2 size={18}/></button></div></header>
}
function Paper({ type = 'Invoice', large = false }: { type?: string; large?: boolean }) {
  return <div className={`paper-wrap ${large ? 'paper-large' : ''}`}><div className="paper-glow"/><div className="paper"><div className="paper-header"><div className="paper-logo">K</div><span>{type}</span><span className="paper-number">#INV-024</span></div><div className="paper-hero"><div><b>INVOICE</b><small>30 NOVEMBER 2024</small></div><strong>৳ 48,500</strong></div><div className="paper-from"><span>FROM</span><b>Kagoj Studio</b><small>Dhaka, Bangladesh</small></div><div className="paper-lines">{[1,2,3,4].map(i => <div key={i}><i/><i/><em/></div>)}</div><div className="paper-total"><span>TOTAL DUE</span><b>৳ 48,500</b></div><div className="paper-footer"><span>Thank you for your business.</span><span>kagoj.studio</span></div></div></div>
}
function TypeSelector({ selected, onSelect }: { selected: string; onSelect: (s: string) => void }) { return <div className="type-grid">{types.map(({ name, icon: Icon, desc, accent }) => <button key={name} onClick={() => onSelect(name)} className={`type-card ${selected === name ? 'selected' : ''}`}><span className={`type-icon ${accent}`}><Icon size={18}/></span><span><b>{name}</b><small>{desc}</small></span>{selected === name && <Check size={15} className="check"/>}</button>)}</div> }
function Home({ setView, selected, setSelected }: { setView: (v: View) => void; selected: string; setSelected: (s: string) => void }) { return <div className="home-view"><section className="hero"><div className="hero-copy"><div className="eyebrow coral"><Sparkles size={13}/> BUSINESS DOCUMENTS, REIMAGINED</div><h1>Make it official<span>.</span></h1><p>Create beautiful business documents in seconds. Designed for the way Bangladesh does business.</p><div className="hero-actions"><button className="button primary" onClick={() => setView('editor')}>Create document <ArrowUpRight size={16}/></button><button className="button ghost" onClick={() => setView('templates')}>Explore templates</button></div><div className="trusted"><div className="avatar-stack"><span>SA</span><span>NS</span><span>TK</span></div><span>Loved by 2,000+ business owners</span></div></div><div className="hero-stage"><div className="stage-caption"><span>LIVE PREVIEW</span><span><Zap size={12}/> interactive</span></div><div className="hero-paper"><Paper large/></div><div className="orbit orbit-a"/><div className="orbit orbit-b"/><div className="floating-note"><Check size={13}/> ready to send</div></div></section><section className="section-block"><div className="section-heading"><div><div className="eyebrow">ONE SPACE, EVERY DOCUMENT</div><h2>Start with what you need.</h2></div><button className="link-button" onClick={() => setView('editor')}>View all <ArrowUpRight size={15}/></button></div><TypeSelector selected={selected} onSelect={setSelected}/></section><section className="split-promo"><div><div className="eyebrow teal">A BETTER WAY TO WORK</div><h2>Your business deserves<br/>better paperwork.</h2><p>From first quote to final receipt, every document carries your brand. No spreadsheets. No soulless templates.</p><button className="button dark" onClick={() => setView('workspace')}>Enter your workspace <ArrowUpRight size={15}/></button></div><div className="mini-stack"><Paper type="Receipt"/><Paper type="Quotation"/></div></section></div> }
function Workspace({ setView }: { setView: (v: View) => void }) { return <div className="workspace-view"><div className="page-intro"><div><div className="eyebrow coral">SATURDAY, 30 NOVEMBER 2024</div><h1>Good morning, Rafi<span>.</span></h1><p>Your documents are looking good today.</p></div><button className="button primary" onClick={() => setView('editor')}><Plus size={17}/> Create new document</button></div><div className="workspace-grid"><button className="create-tile" onClick={() => setView('editor')}><span className="plus-circle"><Plus/></span><b>Create new document</b><small>Start from a blank canvas or a template</small><span className="shortcut">N</span></button><button className="feature-tile" onClick={() => setView('templates')}><div><div className="eyebrow">CURATED FOR YOU</div><h3>Find your<br/><i>signature</i> style.</h3><span className="tile-link">Browse templates <ArrowUpRight size={14}/></span></div><div className="tile-paper"><Paper type="Invoice"/></div></button></div><div className="activity-heading"><div><div className="eyebrow">YOUR SPACE</div><h2>Recent documents</h2></div><button className="link-button">View all <ArrowUpRight size={15}/></button></div><div className="doc-grid">{docs.map(doc => <button className="doc-thumb" key={doc.title} onClick={() => setView('editor')}><div className="thumb-paper"><div className={`thumb-band ${doc.tint}`}/><span className="thumb-brand">K</span><b>{doc.title.split(' ')[0]}</b><small>৳ {doc.amount.replace('৳ ','')}</small><div className="thumb-lines"/></div><div className="thumb-meta"><div><b>{doc.title}</b><small>{doc.client}</small></div><span className={`status ${doc.status.toLowerCase()}`}>{doc.status}</span></div><div className="thumb-hover">Open document <ArrowUpRight size={14}/></div></button>)}</div></div> }
function Editor({ setView, selected, setSelected }: { setView: (v: View) => void; selected: string; setSelected: (s: string) => void }) { const [tab, setTab] = useState('Content'); const [business, setBusiness] = useState('Kagoj Studio'); return <div className="editor-view"><div className="editor-top"><button className="back-button" onClick={() => setView('workspace')}><ArrowLeft size={16}/> Workspace</button><div className="editor-title"><span className="save-dot"/> Untitled invoice <small>Saved just now</small></div><div className="editor-actions"><button className="icon-button"><MoreHorizontal size={18}/></button><button className="button outline" onClick={() => setView('success')}><Download size={15}/> Export</button></div></div><div className="editor-layout"><aside className="editor-sidebar"><div className="sidebar-tabs"><button className={tab === 'Content' ? 'active' : ''} onClick={() => setTab('Content')}>Content</button><button className={tab === 'Style' ? 'active' : ''} onClick={() => setTab('Style')}>Style</button></div>{tab === 'Content' ? <div className="property-stack"><label>Document type</label><button className="select-control" onClick={() => setSelected(selected === 'Invoice' ? 'Receipt' : 'Invoice')}><span>{selected}</span><ChevronDown size={15}/></button><label>Business details</label><div className="field"><span>Business name</span><input value={business} onChange={e => setBusiness(e.target.value)}/></div><div className="field"><span>Customer name</span><input defaultValue="Aarong Interiors"/></div><div className="field-row"><div className="field"><span>Invoice no.</span><input defaultValue="INV-024"/></div><div className="field"><span>Date</span><input defaultValue="30/11/24"/></div></div><label>Line items</label><div className="line-item"><div><b>Design consultation</b><small>1 × ৳ 35,000</small></div><Pencil size={14}/></div><div className="line-item"><div><b>Brand identity system</b><small>1 × ৳ 13,500</small></div><Pencil size={14}/></div><button className="add-line"><Plus size={14}/> Add line item</button><label>Notes</label><textarea defaultValue="Thank you for your business."/></div> : <div className="property-stack"><label>Brand palette</label><div className="color-row"><button className="swatch coral-bg"/><button className="swatch ink-bg"/><button className="swatch teal-bg"/><button className="swatch gold-bg"/><button className="swatch add-swatch"><Plus size={14}/></button></div><label>Layout</label><div className="layout-options"><button className="layout-option active"><LayoutTemplate size={20}/><span>Editorial</span></button><button className="layout-option"><Layers3 size={20}/><span>Compact</span></button></div><label>Typography</label><button className="select-control"><span>DM Serif + DM Sans</span><ChevronDown size={15}/></button><label>Corner radius</label><input className="range" type="range" defaultValue="55"/></div>}</aside><main className="canvas"><div className="canvas-toolbar"><span>100%</span><button>−</button><button>+</button><span className="toolbar-divider"/><button><Settings2 size={15}/></button></div><div className="canvas-paper"><Paper type={selected}/><div className="context-toolbar"><button><Type size={14}/></button><button><Palette size={14}/></button><button><Pencil size={14}/></button><span>Selected: total</span></div></div></main><aside className="inspector"><div className="inspector-head"><span>Quick actions</span><button><X size={15}/></button></div><button className="inspector-action" onClick={() => setView('brand')}><span className="action-icon"><Palette size={16}/></span><span><b>Customize brand</b><small>Make it unmistakably yours</small></span><ArrowUpRight size={14}/></button><button className="inspector-action"><span className="action-icon teal-bg"><Share2 size={16}/></span><span><b>Share with customer</b><small>Send a private preview link</small></span><ArrowUpRight size={14}/></button><div className="inspector-tip"><Sparkles size={15}/><p><b>Pro tip</b><br/>Add your payment details so customers know exactly where to send it.</p></div></aside></div></div> }
function Templates({ setView }: { setView: (v: View) => void }) { const [cat, setCat] = useState('Minimal'); return <div className="templates-view"><div className="page-intro centered"><div><div className="eyebrow coral">THE KAGOJ LIBRARY</div><h1>Templates with a point of view<span>.</span></h1><p>Start with a strong foundation, then make it yours.</p></div></div><div className="category-row">{templates.map(t => <button key={t} className={cat === t ? 'active' : ''} onClick={() => setCat(t)}>{t}</button>)}</div><div className="template-grid">{[1,2,3,4,5,6].map(i => <button className="template-card" key={i} onClick={() => setView('editor')}><div className={`template-preview variant-${i}`}><div className="template-preview-paper"><div className="tp-top"><span>K</span><small>{cat.toUpperCase()}</small></div><h3>{i % 2 ? 'INVOICE' : 'RECEIPT'}</h3><strong>৳ 48,500</strong><div className="tp-lines"/><div className="tp-total">TOTAL <b>৳ 48,500</b></div></div><span className="use-template">Use template <ArrowUpRight size={14}/></span></div><div className="template-info"><b>{cat} / {i % 2 ? 'Quiet line' : 'Soft form'}</b><small>Invoice · Receipt · Quote</small></div></button>)}</div></div> }
function Brand({ setView }: { setView: (v: View) => void }) { return <div className="brand-view"><div className="page-intro"><div><div className="eyebrow teal">YOUR VISUAL IDENTITY</div><h1>Make it yours<span>.</span></h1><p>A brand kit that keeps every document unmistakably you.</p></div><button className="button primary" onClick={() => setView('editor')}>Preview on a document <ArrowUpRight size={16}/></button></div><div className="brand-layout"><div className="brand-controls"><section><div className="control-heading"><b>Logo</b><small>Shown on every document</small></div><div className="logo-upload"><div className="upload-logo">K</div><div><b>Kagoj Studio</b><small>kagoj-logo.svg · 42 KB</small></div><button className="icon-button"><Upload size={15}/></button></div></section><section><div className="control-heading"><b>Colors</b><small>Your signature palette</small></div><div className="brand-colors"><div><button className="large-swatch coral-bg"/><span>Coral</span><small>#E96D52</small></div><div><button className="large-swatch ink-bg"/><span>Ink</span><small>#202321</small></div><div><button className="large-swatch teal-bg"/><span>Teal</span><small>#2E9E8F</small></div><button className="large-swatch add-swatch"><Plus size={18}/></button></div></section><section><div className="control-heading"><b>Typography</b><small>The voice of your business</small></div><div className="font-pair"><div><span className="font-serif">Ag</span><b>DM Serif Display</b></div><div><span>Ag</span><b>DM Sans</b></div><ChevronDown size={15}/></div></section><section><div className="control-heading"><b>Document style</b><small>Choose your visual language</small></div><div className="style-pills"><button className="active">Editorial</button><button>Minimal</button><button>Bold</button></div></section></div><div className="brand-preview"><div className="preview-label"><span>LIVE PREVIEW</span><span><span className="save-dot"/> all changes saved</span></div><Paper large type="Invoice"/></div></div></div> }
function Success({ setView }: { setView: (v: View) => void }) { return <div className="success-view"><div className="success-orb"><Check size={28}/></div><div className="eyebrow teal">DOCUMENT READY</div><h1>That&apos;s a wrap<span>.</span></h1><p>Your invoice is polished, packaged, and ready to meet the world.</p><div className="success-paper"><Paper large/></div><div className="success-actions"><button className="button primary"><Download size={16}/> Download PDF</button><button className="button outline"><Share2 size={16}/> Share link</button><button className="button ghost"><Send size={16}/> Print</button></div><button className="link-button" onClick={() => setView('editor')}><Plus size={15}/> Create another document</button></div> }
export default function Page() { const [view, setView] = useState<View>('home'); const [selected, setSelected] = useState('Invoice'); const screen = useMemo(() => ({ home: <Home setView={setView} selected={selected} setSelected={setSelected}/>, workspace: <Workspace setView={setView}/>, editor: <Editor setView={setView} selected={selected} setSelected={setSelected}/>, templates: <Templates setView={setView}/>, brand: <Brand setView={setView}/>, success: <Success setView={setView}/> }[view]), [view, selected]); return <main className="app-shell"><Nav view={view} setView={setView}/>{screen}</main> }
